'use strict';

describe('myApp.restaurantList module', function() {

  beforeEach(module('myApp.restaurantList'));

  describe('restaurantList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var restaurantListCtrl = $controller('restaurantListCtrl');
      expect(restaurantListCtrl).toBeDefined();
    }));

  });
});