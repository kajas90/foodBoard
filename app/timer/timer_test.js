'use strict';

describe('myApp.timer module', function() {

  beforeEach(module('myApp.timer'));

  describe('timer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var timerCtrl = $controller('timerCtrl');
      expect(timerCtrl).toBeDefined();
    }));

  });
});